"""
[Placeholder] Structured logging framework — not yet implemented.
See docs/next_step_review.md for planned work.

Intended future role:
  Provide a project-wide logging facade (structured JSON logs, per-module
  log levels, optional file sink) replacing ad-hoc print() calls.
"""
raise NotImplementedError(
    "utils.logging is not yet implemented. "
    "Use Python's standard library logging module directly for now."
)
